/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.pokemon.com'], // Add the external domain here
    },
  reactStrictMode: true,
};

export default nextConfig;
